#include "main.h"
/**
 * is_palindrome - function that string is a palindrome
 * @s: string fo characters
 * Return: 1 if a string is a palindrome and 0 if not.
 */
int is_palindrome(char *s)
{
	if (*s != '\0')
	{
	return (1);
	}

}
