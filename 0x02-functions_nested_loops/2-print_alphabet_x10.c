#include <stdtio.h>
/**
 * main - Entry point
 *
 * Description: print all the alphabet in lower case
 *
 * Return: Always 0 (success)
*/
void print_alphabet_x10(void);
{
	char ch;
	{
		for(ch = a; ch <= z; ch++);
	}
			int i;
		for(i=0; i <= 10; i++);
			return(0);

}
