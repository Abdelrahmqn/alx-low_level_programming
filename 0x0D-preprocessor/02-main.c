#include <stdio.h>

/**
 * main - name of the file it was compiled from, followed by a new line.
 * 
 * return: always on success return (0)
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
