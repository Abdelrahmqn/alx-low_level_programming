#include "dog.h"
#include <stdio.h>
/**
 *print_dog - dog's information
 *@d: char
 */

void print_dog(struct dog *d)
{
	if (d)
	{
	if (d == NULL)
		printf("(nill)\n");
	else
		(name == NULL)
			printf("Name: (nil)\n");

	}
}
