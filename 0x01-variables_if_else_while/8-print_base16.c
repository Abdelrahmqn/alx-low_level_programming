#include <stdio.h>
/**
 * main - Entry point
 *
 * description: write the alphabet in lower case then upper case
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	int i;

	for (i = 48; i < 57; i++)
{
		putchar(ch);
		ch++;
}
{
	char ch = 'a';

	while (ch <= 'f')
{
		putchar(ch);
		ch++;
}
	putchar('\n');

}
	return (0);
}
