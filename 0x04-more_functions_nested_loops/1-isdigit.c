#include "main.h"
/**
 * main -Entry point
 *
 * @c: input for alphabet
 *
 * Return: return 1 if c digit or return 0 if not
*/
int _isdigit(int c);
{
	if (c >= 48 && c <= 57)
		
		return (1);
	else
	return (0);
}
